<odoo>
  <record id="mgs_partner_balance_view_id" model="ir.ui.view">
    <field name="name">res.partner.mgs.balance.form</field>
    <field name="model">res.partner</field>
    <field name="inherit_id" ref="base.view_partner_tree" />
    <field name="arch" type="xml">
        <field name="category_id" position="after">
          <field name="mgs_debit" optional="hide"/>
          <field name="mgs_credit" optional="hide"/>
        </field>
    </field>
  </record>

  <!-- <record id="mgs_partner_balance_view_id2" model="ir.ui.view">
    <field name="name">res.partner.mgs.balance.form</field>
    <field name="model">res.partner</field>
    <field name="inherit_id" ref="account_followup.res_partner_view_form" />
    <field name="arch" type="xml">
        <xpath expr="//button[@name='open_partner_ledger']" position="replace">
          <button groups="account.group_account_invoice" type="object" class="oe_stat_button" name="open_partner_ledger" icon="fa-list" invisible="total_due == 0">
              <div class="o_field_widget o_stat_info">
                  <span class="o_stat_value"><field name="mgs_total_due" widget="monetary" options="{'currency_field': 'currency_id'}"/></span>
                  <field name="total_due" invisible="1"/>
                  <span class="o_stat_text">Due</span>
              </div>
          </button>
        </xpath>
    </field>
  </record> -->
</odoo>