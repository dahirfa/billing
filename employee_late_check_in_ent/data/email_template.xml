<?xml version="1.0" ?>
<odoo>
<data noupdate="0">
        <!--Email template -->
        <!-- <record id="email_template_late_check_in" model="mail.template">
            <field name="name">late check in</field>
            <field name="model_id" ref="payroll.model_hr_payslip"/>
            <field name="email_from">{{ user.email_formatted }}</field>
            <field name="email_to">{{ object.employee_id.work_email }}</field>
            <field name="partner_to">{{ object.employee_id.address_home_id.id }}</field>
            <field name="subject">Latency Report</field>
            <field name="body_html" type="html">
            <div style="margin: 0px; padding: 0px;">
                <p style="margin: 0px; padding: 0px; font-size: 13px;">
                    Asc <t t-out="object.employee_id.name"></t>,

                    <br /><br />
                    Here is your latency report between <t t-out="object.date_from"></t>  <t t-out="object.date_to"></t> 
                </p>
            </div>
            </field>
            <field name="report_template" ref="action_late_check_in_report"/>
            <field name="report_name">Latency_report_{{ (object.number or '').replace('/','_') }}</field>
        </record> -->
        </data>
</odoo>