<odoo>
  
  <record id="mgs_partner_balance_view_id" model="ir.ui.view">
    <field name="name">res.partner.mgs.balance.form</field>
    <field name="model">res.partner</field>
    <field name="inherit_id" ref="base.view_partner_tree" />
    <field name="arch" type="xml">
        <field name="category_id" position="after">
          <field name="mgs_debit" optional="hide"/>
          <field name="mgs_credit" optional="hide"/>
        </field>
    </field>
  </record>


  <record id="mgs_billing_account_tree" model="ir.ui.view">
    <field name="name">mgs_billing.billing.account.tree</field>
    <field name="model">res.partner</field>
    <field name="priority">20</field>
    <field name="arch" type="xml">
      <list string="Billing Accounts List" default_order="id desc">
        <field name="property_id" string="House#"/>
        <field name="name"/>        
        <field name="mobile" string="Tenant's Mobile"/>
        <field name="phone" string="Owner's Phone"/>
        <field name="product_id" readonly="1"/>
        <field name="activity_ids" optional="show" widget="list_activity"/>
        <field name="company_id" groups="base.group_multi_company" optional="hide"/>
        <field name="mgs_credit" sum="credit"/>
      </list>
    </field>
  </record>


  <record id="partner_billing_form" model="ir.ui.view">
    <field name="name">res.partner.billing</field>
    <field name="model">res.partner</field>
    <field name="inherit_id" ref="base.view_partner_form"/>
    <field name="arch" type="xml">

      <xpath expr="//div[@name='button_box']/button[1]" position="before">

        <button class="oe_stat_button" name="action_open_reading_history" type="object" icon="fa-clock-o" invisible="not is_tenancy">
          <field string="History" name="reading_history_counter" widget="statinfo"/>
        </button>

        <button class="oe_stat_button" type="object" string="Readings" name="view_readings" icon="fa-tachometer" context="{'partner_id': id}" invisible="not is_tenancy"/>

        <button class="oe_stat_button" name="action_open_customer_payment" type="object" icon="fa-money" invisible="not is_tenancy">
          <field string="Payments" name="payment_counter" widget="statinfo"/>
        </button>


      </xpath>


      <xpath expr="//field[@id='company']" position="attributes">
        <attribute name="force_save">1</attribute>

        <attribute name="readonly">is_tenancy</attribute>
        <attribute name="required">type == 'contact'</attribute>

        <attribute name="invisible">not is_company</attribute>

        <attribute name="column_invisible"/>
      </xpath>

      <xpath expr="//field[@id='individual']" position="attributes">
        <attribute name="force_save">1</attribute>
        <attribute name="readonly">is_tenancy</attribute>
        <attribute name="required">type == 'contact'</attribute>

        <attribute name="invisible">is_company</attribute>
        <attribute name="column_invisible"/>
      </xpath>

      <xpath expr="//span[@name='address_name']" position="before">
        <field name="billing_customer_id" readonly="1" options="{'no_create': True}" invisible="not is_tenancy" required="is_tenancy"/>
        <field name="property_id" readonly="1" options="{'no_create': True}" invisible="not is_tenancy" required="is_tenancy"/>
        <field name="zone_id" readonly="1" invisible="not is_tenancy"/>
        <field name="product_id" readonly="1" options="{'no_create': True}" invisible="not is_tenancy" required="is_tenancy"/>
        <field name="mgs_credit" />
      </xpath>


      <!-- Address Inherit -->
      <xpath expr="//span[@name='address_name']" position="attributes">

        <attribute name="invisible">is_tenancy</attribute>
      </xpath>

      <xpath expr="//div[@class='o_address_format']" position="attributes">

        <attribute name="invisible">is_tenancy</attribute>
      </xpath>

      <xpath expr="//field[@name='vat']" position="before">

        <field name="average_usage" invisible="not is_tenancy" required="is_tenancy"/>
      </xpath>

      <xpath expr="//field[@name='category_id']" position="after">
        <field name="is_tenancy" invisible="1"/>
      </xpath>

    </field>
  </record>



  <record id="res_partner_inherit_alternative_number" model="ir.ui.view">
    <field name="name">billing Addons (Partner)</field>
    <field name="model">res.partner</field>
    <field name="inherit_id" ref="base.view_partner_form"/>
    <field name="arch" type="xml">

      <xpath expr="//field[@name='function']" position="after">
        <field name="alternative_number" string="Sender's Phone" invisible="not is_tenancy"/>
      </xpath>

    </field>
  </record>



  <record id="res_partner_inherit_phone_mobile_tree" model="ir.ui.view">
    <field name="name">billing Phone Mobile (Partner) Tree</field>
    <field name="model">res.partner</field>
    <field name="inherit_id" ref="base.view_partner_tree"/>
    <field name="arch" type="xml">

      <xpath expr="//field[@name='phone']" position="replace">
        <field name="phone" string="Owner's Phone"/>
        <field name="mobile" widget="phone" string="Tenant's Phone"/>
      </xpath>

    </field>
  </record>

  <record model="ir.actions.act_window" id="mgs_billing_tenancy_acc_action">
    <field name="name">Billing Accounts</field>
    <field name="res_model">res.partner</field>
    <field name="view_mode">list,form</field>
    <field name="view_id" ref="mgs_billing_account_tree"/>
    <field name="domain">[('is_tenancy','=', True)]</field>
    <field name="context">{'default_is_tenancy': 1,'default_name': "/"}</field>
    <field name="help" type="html">
      <p class="o_view_nocontent_smiling_face">Create Billing Accounts</p>
    </field>
  </record>


  
</odoo>